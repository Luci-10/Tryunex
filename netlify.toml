[build]
  command = "npm install && mkdir -p dist && cp index.html script.js styles.css supabase-client.js dist/"
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "https://www.tryunex.in/*"
  to   = "https://tryunex.in/:splat"
  status = 301
  force  = true
