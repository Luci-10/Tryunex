<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12 and later read this instead of android:allowBackup.

  Nothing is eligible for backup or device-to-device transfer. The app holds a
  session token and cached photographs of people's clothes and bodies; on some
  devices an allowed backup can be pulled off over adb, so none of it leaves
  the device this way. Signing in again restores everything that matters, all
  of which lives on the server.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
