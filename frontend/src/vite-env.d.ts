/// <reference types="vite/client" />

// Injected by Vite's `define` (see vite.config.ts) from package.json and the
// build timestamp. Used by the About and Settings pages.
declare const __APP_VERSION__: string;
declare const __BUILD_DATE__: string;
